<!-- Generated from the rule modules by scripts/generate_docs.py. Do not edit. -->

# QXL105: circuit with measurements passed to a StatevectorEstimator

- **Name**: `measured-circuit-to-statevector-estimator`
- **Tier**: default
- **Severity**: error
- **Version gated**: no

## Why this is a problem

An Estimator computes expectation values from the statevector and has no use for classical bits. StatevectorEstimator refuses them outright: verified on Qiskit 2.5.1, it raises QiskitError('Cannot apply instruction with classical bits: measure'). This usually appears when a circuit written for a Sampler is reused for an Estimator without removing the measurements.

## When this is legitimate

Never for StatevectorEstimator, which is the only implementation this rule looks at. Other estimators are excluded on purpose: what a Runtime EstimatorV2 does with a measured circuit is decided server side and cannot be established offline, so qxlint says nothing about it rather than guessing. BackendEstimatorV2 is excluded for the same reason.

## Flagged

```python
from qiskit import QuantumCircuit
from qiskit.primitives import StatevectorEstimator
from qiskit.quantum_info import SparsePauliOp

qc = QuantumCircuit(2)
qc.h(0)
qc.measure_all()
StatevectorEstimator().run([(qc, SparsePauliOp('ZZ'))])
```

## Not flagged

```python
from qiskit import QuantumCircuit
from qiskit.primitives import StatevectorEstimator
from qiskit.quantum_info import SparsePauliOp

qc = QuantumCircuit(2)
qc.h(0)
StatevectorEstimator().run([(qc, SparsePauliOp('ZZ'))])
```

## Suppressing

```python
result = compute()  # noqa: QXL105
```

```toml
[tool.qxlint]
ignore = ["QXL105"]
```

## References

- <https://quantum.cloud.ibm.com/docs/api/qiskit/qiskit.primitives.StatevectorEstimator>
