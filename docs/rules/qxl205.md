<!-- Generated from the rule modules by scripts/generate_docs.py. Do not edit. -->

# QXL205: an import of a name Qiskit 1.0 or 2.0 removed

- **Name**: `removed-qiskit-symbol`
- **Tier**: default
- **Severity**: error
- **Version gated**: no

## Why this is a problem

Qiskit 1.0 removed the top level execute, Aer and IBMQ, the opflow and algorithms packages, the vendored providers.aer, and QuantumInstance. Qiskit 2.0 removed assemble and the V1 primitives Sampler, Estimator, BackendSampler and BackendEstimator. Each release was read from the published wheels and each absence confirmed on Qiskit 2.5.2. The import raises before a single line of the script runs, so nothing downstream can be reached.

## When this is legitimate

On a target proven to predate the release that removed the name, the import still works and the rule stays silent. It reads the dotted path of the import rather than the bound name, so a local module of the same name is not touched, and a relative import, which names nothing outside the package, is never considered.

## Flagged

```python
from qiskit import QuantumCircuit, execute, Aer
```

## Not flagged

```python
from qiskit import QuantumCircuit, transpile
from qiskit_aer import AerSimulator
```

## Suppressing

```python
result = compute()  # noqa: QXL205
```

```toml
[tool.qxlint]
ignore = ["QXL205"]
```

## References

- <https://quantum.cloud.ibm.com/docs/migration-guides/qiskit-1.0-features>
